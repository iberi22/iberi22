---
import Layout from '../layouts/Layout.astro';
import ShaderBackground from '../components/ShaderBackground.astro';
import Reveal from '../components/Reveal.astro';
import '../styles/global.css';

const projects = [
  {
    id: "PRJ_01",
    title: "SaberParaTodos",
    url: "https://saberparatodos.space",
    tag: "Educational platform · 16+ countries",
    tech: "Astro, Svelte, TailwindCSS, Supabase",
    desc: "Exámenes de práctica gratuitos y accesibles. Educación para todos.",
  },
  {
    id: "PRJ_02",
    title: "Shelf",
    url: "https://estante-inventario.vercel.app",
    tag: "P2P inventory & POS",
    tech: "React 19, TypeScript, P2P (CRDT)",
    desc: "Inventario offline-first con sincronización P2P y cifrado post-quantum.",
  },
  {
    id: "PRJ_03",
    title: "Hosteler-IA",
    url: "https://hosteler-ia-mu.vercel.app",
    tag: "Field service agent",
    tech: "TypeScript, React, IA",
    desc: "Gestión de servicios de campo con IA, aprobaciones digitales y modo offline.",
  },
  {
    id: "PRJ_04",
    title: "TikTokBoost",
    url: "https://tiktboost.vercel.app",
    tag: "Content analytics engine",
    tech: "TypeScript",
    desc: "Gestor de contenido y métricas TikTok.",
  },
  {
    id: "PRJ_05",
    title: "OrionHealth",
    url: "https://github.com/iberi22/OrionHealth",
    tag: "Medical diagnostic copilot",
    tech: "Flutter, Dart",
    desc: "Asistente de salud privacy-first con SSI on-device y estándares médicos offline.",
  },
];

const openSource = [
  { name: "Xavier", desc: "Memoria Agentic · apertura próximamente", accent: true },
  { name: "Gestalt", desc: "Core Orchestrator", url: "https://github.com/iberi22/gestalt", accent: false },
  { name: "Obscura", desc: "Privacy Layer", url: "https://github.com/iberi22/obscura", accent: false },
  { name: "swal-ui", desc: "Component Library", url: "https://github.com/iberi22/swal-ui", accent: false },
  { name: "Isar Agent Memory", desc: "Vector Storage", url: "https://github.com/iberi22/isar_agent_memory", accent: false },
  { name: "SWAL Agent Rust", desc: "Rust Agent Runtime", url: "https://github.com/iberi22/swal-agent-rust", accent: false },
  { name: "MCP Database Server", desc: "Context Protocol DB", url: "https://github.com/iberi22/mcp-database-server", accent: false },
  { name: "Skills", desc: "Agent Skills Registry", url: "https://github.com/iberi22/skills", accent: false },
];

const stack = ["Rust", "TypeScript", "Dart/Flutter", "Astro", "Python", "React", "PostgreSQL", "Supabase", "Svelte", "P2P", "WebAssembly", "NixOS"];
---

<Layout>
  <ShaderBackground />

  <!-- ============ HERO ============ -->
  <section class="min-h-[80vh] flex flex-col justify-center max-w-4xl pt-20 mb-32">
    <Reveal>
      <div class="font-mono-label text-mono-label text-[#22d3ee] mb-6 flex items-center gap-2">
        <span class="material-symbols-outlined text-sm">terminal</span>
        <span>DEV.SYS // v1.0.0</span>
      </div>
    </Reveal>
    <Reveal delay={80}>
      <h1 class="massive-heading mb-8">
        Brahyan<br />Belalcazar
      </h1>
    </Reveal>
    <Reveal delay={160}>
      <p class="font-body-lg text-body-lg text-[#7e8b8f] max-w-2xl leading-relaxed border-l-2 border-[#3c494c]/50 pl-6">
        Full-stack developer &amp; AI engineer. Fundador de SouthWest AI Labs (SWAL).
        Construyo software open-source, local-first y soberano — la IA debe correr en tu hardware.
      </p>
    </Reveal>
    <Reveal delay={240}>
      <div class="mt-12 flex flex-wrap gap-4">
        <a
          href="https://github.com/iberi22"
          target="_blank"
          rel="noopener noreferrer"
          class="border border-[#3c494c]/30 text-[#cfccc7] px-8 py-3 font-mono-label text-mono-label hover:bg-[#131314] transition-colors flex items-center gap-2 rounded"
        >
          <span class="material-symbols-outlined text-[18px]">code</span>
          GitHub
        </a>
        <a
          href="https://www.linkedin.com/in/brahyansbr"
          target="_blank"
          rel="noopener noreferrer"
          class="text-[#22d3ee] px-8 py-3 font-mono-label text-mono-label hover:underline decoration-[#22d3ee] transition-all flex items-center gap-2"
        >
          LinkedIn
          <span class="material-symbols-outlined text-[18px]">arrow_forward</span>
        </a>
      </div>
    </Reveal>
    <Reveal delay={320}>
      <div class="mt-14 flex flex-wrap gap-2 max-w-2xl">
        {stack.map((t) => (
          <span class="font-mono-code text-mono-code text-[12px] px-3 py-1 rounded border border-[#3c494c]/30 bg-[#131314] text-[#a8a5a1]">{t}</span>
        ))}
      </div>
    </Reveal>
  </section>

  <!-- ============ PROJECTS ============ -->
  <section class="mb-40" id="projects">
    <Reveal>
      <div class="section-head">
        <span class="material-symbols-outlined text-[#22d3ee]">apps</span>
        <h2 class="font-headline-lg text-headline-lg text-[#d8d5d0]">Proyectos</h2>
      </div>
    </Reveal>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {projects.map((p, i) => (
        <Reveal delay={(i % 3) * 100}>
          <a
            href={p.url}
            target="_blank"
            rel="noopener noreferrer"
            class="glass-panel p-6 flex flex-col justify-between h-56 card-hover rounded block"
          >
            <div>
              <div class="font-mono-label text-mono-label text-[#22d3ee] mb-2">{p.id}</div>
              <h4 class="font-headline-md text-headline-md text-[#cfccc7] mb-2">{p.title}</h4>
              <div class="font-mono-code text-mono-code text-[#3c494c] text-sm mb-3">{p.tag}</div>
              <p class="font-body-md text-body-md text-[#7e8b8f] leading-relaxed">{p.desc}</p>
            </div>
            <div class="flex flex-wrap gap-2 mt-4">
              {p.tech.split(', ').map((t) => (
                <span class="font-mono-code text-mono-code text-[11px] px-2 py-0.5 rounded border border-[#3c494c]/30 bg-[#0a0a0b] text-[#7e8b8f]">
                  {t}
                </span>
              ))}
            </div>
          </a>
        </Reveal>
      ))}
    </div>
  </section>

  <!-- ============ OPEN SOURCE ============ -->
  <section class="mb-40" id="stack">
    <Reveal>
      <div class="section-head">
        <span class="material-symbols-outlined text-[#22d3ee]">code_blocks</span>
        <h2 class="font-headline-lg text-headline-lg text-[#d8d5d0]">Open Source</h2>
      </div>
    </Reveal>
    <Reveal delay={100}>
      <div class="border border-[#3c494c]/20 bg-[#131314] p-8 rounded">
        <p class="font-mono-code text-mono-code text-[#7e8b8f] mb-8 leading-relaxed">
          /* Open-source infrastructure for local-first AI.
             Rust for performance-critical agent logic and memory management. */
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-y-8 gap-x-6">
          {openSource.map((p) => (
            p.url ? (
            <a
              href={p.url}
              target="_blank"
              rel="noopener noreferrer"
              class={`border-l pl-4 ${p.accent ? 'border-[#22d3ee]/50' : 'border-[#3c494c]/30'} hover:border-[#22d3ee] transition-colors group`}
            >
              <div class={`font-body-md text-body-md font-medium mb-1 group-hover:text-[#22d3ee] transition-colors ${p.accent ? 'text-[#22d3ee]' : 'text-[#d8d5d0]'}`}>
                {p.name}
              </div>
              <div class="font-mono-label text-mono-label text-[#7e8b8f] text-[11px]">{p.desc}</div>
            </a>
            ) : (
            <div class={`border-l pl-4 ${p.accent ? 'border-[#22d3ee]/50' : 'border-[#3c494c]/30'}`}>
              <div class={`font-body-md text-body-md font-medium mb-1 ${p.accent ? 'text-[#22d3ee]' : 'text-[#d8d5d0]'}`}>
                {p.name}
              </div>
              <div class="font-mono-label text-mono-label text-[#7e8b8f] text-[11px]">{p.desc}</div>
              <span class="font-mono-label text-mono-label text-[10px] mt-1 text-[#7e8b8f] border border-[#3c494c]/30 rounded px-1.5 py-0.5 inline-block">PRIVATE</span>
            </div>
            )
          ))}
        </div>
      </div>
    </Reveal>
  </section>

  <!-- ============ ABOUT ============ -->
  <section class="mb-32" id="about">
    <Reveal>
      <div class="section-head">
        <span class="material-symbols-outlined text-[#22d3ee]">person</span>
        <h2 class="font-headline-lg text-headline-lg text-[#d8d5d0]">Sobre mí</h2>
      </div>
    </Reveal>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {[
        {
          icon: "dns",
          title: "Local-first",
          text: "Ejecución en el edge. Modelos optimizados para inferencia en hardware de consumo. Latencia ultra baja sin dependencia de red externa.",
        },
        {
          icon: "shield_lock",
          title: "Privacy-first",
          text: "Tus datos, tus pesos. Arquitecturas que garantizan aislamiento criptográfico de contextos y memorias agenticas (Xavier).",
        },
        {
          icon: "hub",
          title: "Soberano",
          text: "Protocolos P2P, código abierto AGPL-3.0 y gobernanza transparente. Sin lock-in de nube ni dependencia de terceros.",
        },
      ].map((p, i) => (
        <Reveal delay={i * 100}>
          <div class="p-8 border border-[#3c494c]/20 bg-[#131314] card-hover rounded">
            <div class="w-12 h-12 bg-[#0a0a0b] flex items-center justify-center border border-[#3c494c]/30 mb-6 rounded">
              <span class="material-symbols-outlined text-[#cfccc7]">{p.icon}</span>
            </div>
            <h3 class="font-headline-md text-headline-md text-[#d8d5d0] mb-4">{p.title}</h3>
            <p class="font-body-md text-body-md text-[#7e8b8f]">{p.text}</p>
          </div>
        </Reveal>
      ))}
    </div>

    <Reveal delay={150}>
      <blockquote class="mt-16 max-w-3xl mx-auto text-center">
        <p class="font-headline-lg text-headline-lg text-[#d8d5d0] leading-snug">
          "La premisa principal es la comodidad de todos los seres humanos en la Tierra, brindando tecnología segura, abierta e inteligente."
        </p>
        <cite class="block mt-6 font-mono-label text-mono-label not-italic text-[#7e8b8f]">— Brahyan Belalcazar "ElBeRi"</cite>
      </blockquote>
    </Reveal>
  </section>
</Layout>
